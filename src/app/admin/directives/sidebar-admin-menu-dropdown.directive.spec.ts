import { SidebarAdminMenuDropdownDirective } from './sidebar-admin-menu-dropdown.directive';

describe('SidebarAdminMenuDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarAdminMenuDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
