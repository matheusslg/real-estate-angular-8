import { SidebarAdminMenuActiveDirective } from './sidebar-admin-menu-active.directive';

describe('SidebarAdminMenuActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarAdminMenuActiveDirective();
    expect(directive).toBeTruthy();
  });
});
