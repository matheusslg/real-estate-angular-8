import { UploadFilesThumbnailDirective } from './upload-files-thumbnail.directive';

describe('UploadFilesThumbnailDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadFilesThumbnailDirective();
    expect(directive).toBeTruthy();
  });
});
