import { PropertyDescriptionLimitPipe } from './property-description-limit.pipe';

describe('PropertyDescriptionLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyDescriptionLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
