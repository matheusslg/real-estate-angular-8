import { SearchPropertyPipe } from './search-property.pipe';

describe('SearchPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
