import { FilterPropertiesPipe } from './filter-properties.pipe';

describe('FilterPropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
